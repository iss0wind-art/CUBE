# CUBE — 3D 공간형 코딩 환경 (Architect OS)

> tmux가 화면을 2D 타일로 쪼갠다면, CUBE는 사용자를 큐브 방 안에 넣고
> 여섯 면 전체를 작업 공간으로 쓴다. ORCA(Hundred Rabbits)처럼
> 격자 하나하나가 살아있는 유닛이다.

## 핵심 개념

1. **구체 프랙탈** — 우리는 큐브 안에 있다. 큐브 안에 큐브가 중첩될 수 있다.
2. **ORCA 격자** — 격자 하나하나가 터미널이다.
3. **정면 벽 = 메인 창** — 현재 작업 중인 터미널 (CLI든 GUI든).
4. **좌/우/천장 격자** — 각 셀은 터미널이 될 수도, 메뉴가 될 수도 있다. 사용자 자유.
5. **세계수 (바닥)** — 처음엔 조그만 애기나무 한 그루. 프로젝트가 진행될수록 자란다.
   각 프로젝트의 *진짜* git worktree이며, 브랜치가 3차원으로 뻗어 한눈에 보인다.
   - 아래로 숨겼다 뺐다 자유
   - 2D로 축소해 좌/우측 벽의 큐브 하나에 도킹 가능
   - 좌측 큐브(백엔드 작업)가 세계수의 한 가지와 연결되고,
     우측 큐브(프론트엔드 작업)가 다른 가지를 점유하는 식
6. **큐브 = 서랍** — 언제든 끼웠다 뺐다, 위치 이동 자유.
7. **모델링 바닥** — 3D 모델링 사용자는 바닥을 간단한 모델링 툴로 쓸 수 있다.
8. **마인드맵 연결** — 좌측 1번 큐브를 우측 9번 큐브로 드래그하면 가상의 선이
   나타나고 둘이 연결된다 (프로젝트 연계). 선 표시/숨김은 토글.
9. **후방 포털** — remote-ssh, 다른 차원(호스트)으로의 이동.
   협업자의 연결선들이 포털을 통해 오간다.
10. **별 = 다른 사람의 큐브** — 큐브 밖 우주에 보이는 별들은 전부
    다른 사용자의 큐브다. 별 하나 = 접속 가능한 차원 하나.
    포털 워프 = 그 별(큐브)로의 이동. 멀티플레이어의 세계관.
11. **모바일도 같은 개념.**

## 구역제 + 도킹 결정 (2026-07-16)

- **구역제(기본값) + 자유 이동**: 천장 = 메뉴 존 (메인에서 가까운 줄부터
  자동 스택, 드래그로 자유 이동), 좌/우 벽 = 터미널 존, 바닥 = 세계수 존.
- **메인 화면 4분할 (그룹 기반)**: 분할 열은 세션의 출신 벽을 따른다 —
  좌벽 세션은 왼쪽 열, 우벽 세션은 오른쪽 열 (공간 기억 = 화면 배치).
  중립 세션(CORE/천장/바닥)만 빈 쪽으로. 열당 최대 2개 = 총 4분할,
  꽉 차면 같은 열의 가장 오래된 패널이 벽으로 복귀.
- **+ NEW 자동 배치**: 셀 지정 없이 만들면 세션 적은 쪽 벽의
  메인에서 가장 가까운 빈 셀부터 차곡차곡.
- 패널 × = 벽으로 되돌리기(세션 유지), 셀 우클릭 = 종료.

## 카메라 UX 결정 (2026-07-16)

- 작업 중에도 회전은 기본 허용 (공간감이 이 툴의 정체성).
- 대신 **잠금 토글** (헤더 자물쇠 버튼 / Ctrl+Shift+L): 회전·줌 동결 + 정면 스냅.
- **모니터 프레임/주변 벽 클릭 = 정면 정렬 스냅** (터미널 내부 클릭은 제외).

## 로드맵

- [x] Phase 0: AI Studio 셸 (CSS 3D 큐브 룸)
- [x] Phase 1: 터미널의 방 — node-pty 백엔드, 정면 벽 메인 터미널,
      격자 셀 클릭으로 터미널 생성/스왑 (PowerShell)
- [x] Phase 2 (MVP): 세계수 — 진짜 git 데이터(브랜치·워크트리·커밋 수)로
      3D 나무 생성 (줄기 높이=총 커밋, 가지=브랜치 황금각 나선, 길이=커밋 수,
      색: 현재 브랜치=액센트/워크트리=초록/일반=회색), 30초 폴링 성장,
      가지 클릭 → 그 워크트리 경로의 터미널 도킹. API는 별도 프로세스(:3002,
      server/tree.ts, CUBE_PROJECTS 환경변수로 프로젝트 등록)
- [ ] Phase 2.5: 세계수 숨김/벽 도킹, 다중 프로젝트 등록 UI, 커밋 그래프 가지치기
- [ ] Phase 3: 마인드맵 연결선 — 셀 간 드래그 연결 + 토글, 상태 저장
- [ ] Phase 4: 포털 — SSH 프로필 → 원격 차원 워프
- [ ] Phase 5: 모델링 바닥 + 모바일 PWA

## 아키텍처

```
[브라우저]                          [로컬 서버 :3001]
React 19 + CSS 3D (큐브 룸)   ⇄    Express + ws
xterm.js (벽면 터미널)        WS    @lydell/node-pty (PowerShell)
                                    (Phase 2+: git 데이터 수집)
```

- CSS 3D를 유지하는 이유: xterm.js는 DOM이므로 CSS 3D 벽에 그대로 붙는다.
  WebGL 씬이었다면 터미널 임베드가 지옥이 된다.
- 실행: `npm run dev` (web + pty 서버 동시 기동)
- 조작: 휠 = 줌, 휠버튼 드래그 = 시점 회전, 셀 클릭 = 터미널 생성/포커스,
  셀 우클릭 = 터미널 종료

## 카메라 설계 (Phase 1.5 — 내부 360° 완성)

- **COORDS (기본, 내부 1인칭)**: 눈 = 방 정중앙, 광각 렌즈(perspective 700px).
  yaw 무제한 360°, pitch ±55°, 휠 = 방 안에서 앞뒤 걷기(±250px).
- **ORBIT (전지적)**: 외부 궤도 카메라. 자유 회전 + 줌 0..400 (perspective 1200px).
- **핵심 아키텍처**: Chrome은 시점 평면(z=perspective)을 가로지르는 요소를
  서브트리째 컬링한다. 해법 = cube-room을 0×0 앵커로 만들고 4개 격자벽의
  256개 셀을 각각 독립 3D 평면으로 배치(transform-origin 트릭으로 기존 기하 재현).
  이러면 머리 뒤의 셀만 개별적으로 사라지고 방은 어느 각도에서도 닫혀 보인다.
  (플레이라이트 순수 실험으로 검증 후 적용 — pt2-yaw* 스크린샷)
- 셀의 팝/호버 깊이는 내부 `.cell-3d` 래퍼가 담당 (배치 transform 보호).
- 메인 터미널 = 벽 중앙 560×470 "모니터 패널".
- **잠금 토글** (헤더 자물쇠 / Ctrl+Shift+L): 회전·줌 동결 + 정면 스냅.
- **모니터 프레임/주변 벽 클릭 = 정면 정렬** (터미널 내부 클릭 제외).
- 휠버튼 드래그는 터미널 위에서도 동작 (왼클릭만 터미널이 소유).
